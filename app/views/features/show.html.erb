<%= title @feature.title%>
<p id="release">Released in the year <%= @feature.release %>.</p>
<h3>Director</h3>
<p id="director"><%= link_to @feature.director, person_path(@feature.director.id) %></p>
<h3>Cast</h3>
<ul id='cast'>
    <%- @feature.actors.each do |actor| -%>
    <li><%= link_to actor, person_path(actor.id) %></li>
    <%- end -%>
</ul>